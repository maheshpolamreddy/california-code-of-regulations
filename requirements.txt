# Core Dependencies
# crawl4ai>=0.3.0  <-- Uncomment for local crawling. Removed for Vercel (too huge).
supabase>=2.0.0
openai>=1.0.0
# google-generativeai>=0.3.0 <-- Removed (Heavy gRPC dependency). Replaced with lightweight REST calls.
python-dotenv>=1.0.0

# Data Processing
markdownify>=0.11.0
pydantic>=2.0.0
beautifulsoup4>=4.12.0
# lxml>=4.9.0  <-- Removed to save space (BS4 defaults to python parser if missing)

# Utilities
requests>=2.0.0
tenacity>=8.2.0
# rich>=13.0.0 <-- Removed (CLI only)
# tqdm>=4.66.0 <-- Removed (CLI only)
aiohttp>=3.9.0
# numpy>=1.24.0  <-- Removed for Vercel (Replaced with pure python math)
# tiktoken>=0.5.0 <-- Removed for Vercel (Indexing only)

# Testing
# pytest>=7.4.0        <-- Removed for prod
# pytest-asyncio>=0.21.0 <-- Removed for prod

# Web Server
Flask>=2.0.0
flask-cors>=3.0.0
gunicorn>=20.1.0
